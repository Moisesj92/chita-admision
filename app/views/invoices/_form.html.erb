<%= form_with(model: invoice) do |form| %>
  <% if invoice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
        <% invoice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :owner_identification, "Rut Emisor" %>
  <%= form.text_field :owner_identification %>

  <%= form.label :recipient_identification, "Rut Receptor" %>
  <%= form.text_field :recipient_identification %>

  <%= form.label :amount, "Monto" %>
  <%= form.text_field :amount %>

  <%= form.label :invoice_identification, "Folio" %>
  <%= form.text_field :invoice_identification %>

  <%= form.label :due_date, "Fecha de vencimiento" %>
  <%= form.date_field :due_date %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
